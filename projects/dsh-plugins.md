# DSH plugins

Two plugins for DeepSeek Harness. They install independently.

## dsh-code-reference

Before implementation: survey local + open source, present reuse / adapt / dependency / rewrite, **ask**. Architecture similarity is a heuristic. Unknown license + whitelist ⇒ block. Deploy policy is a ceiling.

[Repo](https://github.com/victorzhong0110/dsh-code-reference) · Awesome DSH Plugin listing.

## dsh-outcome-loop

After implementation: a task is not a boolean. Mechanical verification × user disposition. Default: no extra LLM calls, no network, no proactive commands. Stale evidence never implies pass.

[Repo](https://github.com/victorzhong0110/dsh-outcome-loop)

Together they are the pre/post pair: candidate discovery, then factual acceptance. Neither is a ByteDance-scale product. Both are the kind of systems work I want an intern team to put in production.
